# RGB LED インタラクティブゲーム仕様書

版: 1.0

基準日: 2026-09-04

状態: 現行実機を基準とする正典

`spec.md` がこのプロジェクトの唯一の正典である。判断に迷ったら推測せず該当節を読む。
本ファイルとREADME、計画書、接続資料、サービス雛形、実装が矛盾した場合は、本ファイルを正とする。
実機で確認された値とリポジトリが異なる箇所は、§10の既知ギャップとして扱う。

## §1 Overview

STRUCTURE Sensorで人物の位置・動作を認識し、縦型RGB LED表示上のゲームを操作する。
深度処理とゲーム制御を別のRaspberry Piへ分離し、表示専用LAN上の3台の表示Piで
192×384の画面を駆動する。

## §2 Product Principles

1. 現行実機で動作する3台構成を優先し、旧構成との互換性を要件にしない。
2. 深度画像・人物マスクはセンサーPi内に留め、判定済み入力だけを送信する。
3. 表示前にCRC、チャンク完全性、パレット範囲を検証する。
4. パレット、識別子、接続順、校正値を推測で変更しない。
5. P0が実機で通るまでP1・P2へ進まない。
6. 期間内に実機検証まで完了できない機能はP0へ追加しない。
7. テスト成功だけで完了とせず、対象操作を実機で最低1回確認する。

## §3 Scope

### §3.1 P0 — 現行運用に必須

- [x] センサーPiで深度背景を学習する。
- [x] 人物候補を抽出・追跡し、操作対象を1人にロックする。
- [x] 判定済み入力をUDP 5200で制御Piへ送る。
- [x] 制御Piで192×384のゲーム画面を60fpsで更新する。
- [x] MSX16パレットで描画する。
- [x] 192×128の3領域へ分割し、表示Pi 3台へUDP 5000で送る。
- [x] 表示Piで受信検証、RGB変換、パネル補正、HUB75出力を行う。
- [x] 表示PiからUDP 5101で死活・FPS・欠損数・温度を返す。
- [x] ミスまたはステージ切替時に人物を再選択する。
- [ ] リポジトリを現行実機の3台・192×128構成へ同期する。
- [ ] 同期後のコードを全実機へ書き込み、ゲームを通しで確認する。

### §3.2 P1 — P0完了後

- [ ] READY応答をUDP 5100で実装する。
- [ ] 全表示Piの準備完了を待ってフレーム切替を開始する。
- [ ] センサー設定UIと現行サービスの操作を統合する。

### §3.3 P2 — 将来

- [ ] GPIOによる物理同期パルスを導入する。
- [ ] フレーム配信の欠損回復または再送方式を評価する。
- [ ] 配備とロールバックを自動化する。

## §4 Physical Architecture

### §4.1 ノード

| 役割 | ホスト | IP | プロセス／サービス |
|---|---|---|---|
| センサー | `pi3-sensor` | `192.168.50.33` | `sensor_agent.py` / `pi3-sensor.service` |
| 制御 | `pi3-control` | `192.168.50.32`, `192.168.10.2` | `block_breaker.py` / `pi3-control.service` |
| 表示1 | `pi1` | `192.168.10.101` | `pi-client@0.service` |
| 表示2 | `pi4` | `192.168.10.104` | `pi-client@1.service` |
| 表示3 | `pi2` | `192.168.10.102` | `pi-client@2.service` |

### §4.2 LED配置

- パネル: 32×32 RGB LED、合計72枚
- 表示Pi 1台: `chain_length=8`, `parallel=3`、24枚、192×128
- 全体: 表示領域3段、192×384
- 論理分割順: target 0 → target 1 → target 2
- 宛先順: `.101` → `.104` → `.102`

## §5 Network Architecture

| 用途 | 方向 | プロトコル | ポート |
|---|---|---|---:|
| 判定済み入力 | センサーPi → 制御Pi | UDP | 5200 |
| 人物再選択 | 制御Pi → センサーPi | UDP | 5201 |
| LEDフレーム | 制御Pi → 表示Pi | UDP | 5000 |
| 表示Pi死活 | 表示Pi → 制御Pi | UDP | 5101 |
| READY応答 | 表示Pi → 制御Pi | UDP | 5100、未実装 |
| 管理 | 開発機 → 各ノード | TCP/SSH | 22 |

表示LANは `192.168.10.0/24` とし、フレーム配信以外の常時トラフィックを持ち込まない。
センサー・制御間は `192.168.50.0/24` を使う。

## §6 Sensor Logic

1. OpenNI2で深度フレームを取得する。
2. 設置方向に応じて上下・左右を反転し、ROI適用後に処理解像度へ縮小する。
3. 起動後の無人フレームから画素ごとの中央値背景とノイズP95を求める。
4. 背景より手前へ変化した画素だけを前景候補とする。
5. OPEN、CLOSE、時間方向の合意処理でノイズを除く。
6. 面積・形状・継続性を満たす領域を人物候補にする。
7. 候補を時系列追跡し、最も手前の安定人物を操作対象としてロックする。
8. `body_x`、`lateral`、`jump`、`start_trigger`、人物情報を `InputState` にする。
9. 現行開始モード `still` では、横位置を保った3秒静止を開始トリガーにする。
10. 入力パケットへsequenceとCRC32を付け、UDP 5200へ送信する。

入力が0.50秒を超えて途絶えた場合、制御Piは無入力として扱う。イベントフラグは
新しいsequenceに対して一度だけ消費し、古いsequenceと不正CRCは破棄する。

## §7 Game Logic

### §7.1 メインループ

- 更新周期は60fps。
- 入力値からパドル位置を更新する。現行の有効範囲は正規化座標 `0.15..0.85`、追従ゲインは `0.9`。
- 開始トリガー後にカウントダウンし、ボールを発射する。
- `dt` は最大40msへ制限し、ボール移動は最大8サブステップへ分割する。
- 各サブステップで壁、上端、パドル、ボス／バリア、落下の順に判定する。
- ミス後に残機があれば人物ロックを解除し、再選択と静止開始を待つ。
- 残機ゼロではGAME OVERを表示後、ゲームをリセットする。
- 通常ステージ完了後はボス戦へ遷移し、ステージ切替時にも人物を再選択する。

### §7.2 描画

- 論理キャンバスは192×384、1画素1バイトのパレットインデックスとする。
- 現行実機の出力パレットはMSX16とする。
- インデックス0の透明色は送信前に背景へ解決する。
- 送信前に形状とパレット範囲を検証する。

## §8 Frame Transport and Display Logic

1. 完成フレームを上から192×128の3領域へ分割する。
2. target 0を`.101`、target 1を`.104`、target 2を`.102`へ送る。
3. 各領域を既定1200バイトで分割し、frame ID、target ID、palette mode、chunk ID、
   chunk count、payload size、CRC32を付ける。
4. 表示Piは自target以外、形式不正、CRC不一致、重複、不完全フレームを破棄する。
5. 全チャンクが揃ったフレームだけをパレットLUTでRGBへ変換する。
6. パネル校正を適用し、VSyncに合わせて表示バッファを交換する。
7. 表示Piは1秒ごとに表示数、欠損数、FPS、稼働時間、温度をUDP 5101へ送る。

## §9 Implementation Rules

1. `spec.md` の改訂なしにノード数、IP、target ID、表示順を変更しない。
2. 現行の宛先順 `.101`, `.104`, `.102` をIP順へ並べ替えない。
3. 深度画像・人物マスクをセンサーPi外へ送らない。
4. パレット値の正本は `host/palettes.py` と `host/palettes.json` とする。
5. UDP受信時のCRC・サイズ・sequence・範囲検証を省略しない。
6. 実機へ書き込む前に対象ファイルと差分を列挙する。
7. 配備前にselftest、構文／ビルド、実機スモークの順で検証する。
8. P0の実機スモークが通るまでP1・P2を実装しない。
9. plan、tasksなどの派生文書は根拠となる本仕様の節番号を `§N` 形式で引用する。
10. 旧構成の手順や値を現行文書へ混在させない。

## §10 Known Repository Gaps

2026-09-04の実機読み取り結果に対し、ローカル作業ツリーには次の未同期がある。

| 対象 | 現行実機 | ローカル作業ツリー |
|---|---|---|
| 表示Pi数 | 3 | `PI_COUNT=4` |
| 1台の表示領域 | 192×128 | 192×96 |
| 表示クライアント | chain 8、parallel 3 | chain 6、parallel 3 |
| 制御サービス | 60fps、MSX16、3宛先 | 30fps、MSX16、4宛先 |
| target割当 | `.101=0`, `.104=1`, `.102=2` | `.101=0`, `.102=1`, `.103=2`, `.104=3` |

ローカルの未コミット変更 `host/block_breaker.py` と `host/block_breaker_selftest.py` は
別作業の成果であり、この文書更新では変更しない。
センサーPiは制御Pi経由のSSH公開鍵認証が通らず、稼働サービスの直接確認は未実施である。

## §11 Build Order

### Phase 0 — 現行差分の固定

Goal: 実機とローカルの差分をバックアップ付きで再現可能にする。

- 実機ファイルとsystemd設定を取得する。
- ローカル未コミット変更と衝突しない差分計画を作る。

Gate: 3台・192×128・target割当の根拠が保存されていること。

### Phase 1 — リポジトリ同期

Goal: ローカルコードとサービス雛形を現行3台構成へ一致させる。

- フレーム分割定数を3台・128行へ更新する。
- 表示クライアントをchain 8・parallel 3へ更新する。
- サービス雛形を60fps、3宛先、現行target割当へ更新する。
- selftestを新しい分割へ更新する。

Gate: 全selftestと表示クライアントのビルドが成功すること。

### Phase 2 — 実機書き込み

Goal: 正典と一致するコードを5台へ安全に反映する。

- 制御Pi、センサーPi、表示Pi 3台の順に対象差分を確認する。
- 既存ファイルをバックアップしてから配備する。
- サービスを再起動し、active状態とログを確認する。

Gate: 全サービスがactiveで、入力と全画面表示が継続すること。

### Phase 3 — 実機スモーク

Goal: 人物入力からLED表示まで端から端を確認する。

- 背景学習、人物選択、3秒静止、ゲーム開始を確認する。
- 左右追従、ボール反射、ミス後の人物再選択を確認する。
- 3領域の順序、欠け、色、FPS、欠損数を確認する。

Gate: §12の全ACが合格すること。

## §12 Acceptance Criteria

### Functional

- [ ] 表示順が上から `.101`, `.104`, `.102` である。
- [ ] 各表示Piが192×128を表示し、全体が192×384になる。
- [ ] センサー前の人物がパドルへ追従する。
- [ ] 3秒静止でゲームが開始する。
- [ ] ミス後に人物が再選択される。
- [ ] MSX16範囲外の画素を送信・表示しない。

### Transport

- [ ] CRC不一致、不完全、重複、他targetのチャンクを表示しない。
- [ ] 入力切断から0.50秒以内に無入力状態へ移る。
- [ ] 3台からUDP 5101の死活報告を受信する。

### Verification

- [ ] Python selftestがすべて成功する。
- [ ] `pi_client` が3台用設定でビルドできる。
- [ ] 制御Pi、センサーPi、表示Pi 3台のサービスがactiveである。
- [ ] 実機で背景学習からゲームオーバーまたはステージ遷移まで1回完走する。
- [ ] `git status` と `git diff` で意図しない変更がない。

## §13 Definition of Done

STRUCTURE Sensorの背景学習 → 人物検出と操作対象ロック → 3秒静止による開始 →
センサーPiから制御Piへの入力送信 → 60fpsのゲーム更新 → 192×128×3領域のUDP配信 →
表示Piでの検証・RGB変換・192×384 LED表示 → ミス後の人物再選択、という一連の流れが
実機で完走し、§12の全項目が合格した状態を完了とする。

READYバリア、GPIO物理同期、再送方式はDoDに含めない。

## §14 Final Priority

判断に迷った場合は次の順で優先する。

1. 実機を壊さず、現在動いている状態を維持できるか。
2. 192×384の全表示と人物操作が実際に動くか。
3. 現行のノード・target・表示順と一致するか。
4. 入力・フレームの検証境界を維持できるか。
5. 配備とロールバックを再現できるか。
6. 実装が簡潔か。
