取得日: 2026-08-21

親機 `oyaki` からのICMP疎通:

```text
192.168.10.101 PING_OK
192.168.10.102 PING_OK
192.168.10.103 PING_OK
192.168.10.104 PING_OK
```

PiへのSSHは、開発機からの直結および親機経由の既知ユーザー（`pi`, `root`, `th1`, `ubuntu`）で公開鍵認証に失敗した。Piの実ファイル・実行バイナリは回収できていない。

Pi側の変更対象としてGitに保存済みの正本は `pi-client/` である。
